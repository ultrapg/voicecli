[package]
name = "voicecli"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
pyo3 = { version = "0.22", features = ["auto-initialize", "generate-import-lib"] }
